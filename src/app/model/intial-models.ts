import { environment } from 'src/environments/environment';


export class InitialModels{
    public static username = environment.auth_username;
    public static password = environment.auth_password;
}